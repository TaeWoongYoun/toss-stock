# Toss Stock CLI — 실행에 필요한 파이썬 패키지
# 설치:  pip install -r requirements.txt
# (start.bat / start.sh 를 쓰면 자동으로 설치됩니다)

httpx>=0.27,<1.0      # 토스증권 API 호출
textual>=0.60         # TUI 대시보드(app.py). CLI(cli.py)만 쓰면 없어도 됩니다.
